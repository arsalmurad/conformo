{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM"],
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "allowImportingTsExtensions": true,
    "noEmit": true,
    "types": ["node"],
    "paths": {
      "@verinvoice/core": ["./packages/core/src/index.ts"],
      "@verinvoice/formats": ["./packages/formats/src/index.ts"],
      "@verinvoice/pdf": ["./packages/pdf/src/index.ts"],
      "@verinvoice/validate": ["./packages/validate/src/index.ts"],
      "@verinvoice/validate/browser": ["./packages/validate/src/browser.ts"],
      "@verinvoice/validate/coverage": ["./packages/validate/src/coverage.ts"],
      "@verinvoice/cli": ["./packages/cli/src/cli.ts"],
      "@verinvoice/parse": ["./packages/parse/src/index.ts"],
      "@verinvoice/compliance-data": ["./packages/compliance-data/src/index.ts"]
    },
    "baseUrl": "."
  },
  "include": ["packages/*/src/**/*.ts", "tools/**/*.ts", "test/**/*.ts", "e2e/**/*.ts", "playwright.config.ts"],
  // pdfjsWorker.ts uses Vite's `?url` import convention, typed only by
  // vite/client (which this Node-context config deliberately doesn't set,
  // since it also checks tools/ and test/, which run under Node, not Vite).
  // packages/ui's own tsconfig.app.json (chained after this one in the root
  // "typecheck" script) has vite/client and checks this file correctly.
  "exclude": ["packages/ui/src/pdfjsWorker.ts"]
}
